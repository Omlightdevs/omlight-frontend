export interface ILightProps {
  _id: string;
  images: string;
  title: string;
  description: string;
  price?: string;
}

export interface INewLightProps {
  images: string;
  title: string;
  description: string;
  price?: string;
}
