import React from "react";

import { AdminNavs } from "../../../layout";
import { Typography } from "@material-ui/core";

export const WebsiteInformation = () => {
  return (
    <AdminNavs>
      <Typography variant="h1">Admin information</Typography>
    </AdminNavs>
  );
};
